// import TracksService from '../../../modules/Catalog/Tracks/tracks.service';
// import { prisma } from '../../../config/db';
// import { track } from '@prisma/client';

jest.mock('../../../config/db', () => {});

describe('Track sub module', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });
});
